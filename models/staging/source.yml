version: 2

sources:
  - name: raw
    description: "Raw data loaded from CSV seeds"
    tables:
      - name: customers_raw
        description: "Raw customer data"
      - name: orders_raw
        description: "Raw order data"
      - name: products_raw
        description: "Raw product data"
